---
import ActionLink from '@/components/ActionLink.astro';
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getSiteContent } from '@/lib/sanity';

const { settings, businessHours, specialHours } = await getSiteContent();
---

<BaseLayout
  title="페이지를 찾을 수 없습니다"
  description="요청한 와플톡 페이지를 찾을 수 없습니다. 메뉴를 확인하거나 온라인 주문으로 이동하세요."
  settings={settings}
  businessHours={businessHours}
  specialHours={specialHours}
>
  <section class="shell not-found">
    <p class="section-kicker">페이지를 찾을 수 없어요</p>
    <h1>주소가 바뀌었거나<br />없는 페이지예요</h1>
    <p>메뉴를 확인하거나 홈으로 돌아가 주세요.</p>
    <div class="action-row">
      <ActionLink href="/menu" label="메뉴로 돌아가기" icon="menu" />
      <ActionLink href="/" label="홈으로 이동" icon="arrow" variant="outline" />
    </div>
  </section>
</BaseLayout>

<style>
  .not-found {
    display: grid;
    min-height: 65vh;
    gap: 1rem;
    align-content: center;
    justify-items: start;
    padding-block: 4rem;
  }

  .not-found p:not(.section-kicker) {
    max-width: 35rem;
    color: var(--color-fg-neutral-muted);
  }
</style>
